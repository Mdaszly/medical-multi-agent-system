# 医疗平台前端

Vue 3 + Vite + Element Plus。完整说明见 **[docs/HANDBOOK.md](../docs/HANDBOOK.md)**。

```powershell
npm install
npm run dev      # 开发，代理后端 http://localhost:8080
npm run build    # 产物 dist/（不提交 Git）
```
